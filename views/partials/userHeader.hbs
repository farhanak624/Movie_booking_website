<header class="navigation" style="z-index: 1;">
    <nav class="navbar navbar-expand-lg navbar-light bg-dark h-3" >
      <a class="navbar-brand text-light" href="#"><i class="fa-solid fa-clapperboard"></i> Get My Show</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item active mr-5">
            <a class="nav-link text-light" href="/"><i class="fa-solid fa-house"></i> Home<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item active mr-5">
            <a class="nav-link text-light" href="#"><i class="fa-solid fa-pen"></i> Features</a>
          </li>
          <li class="nav-item active mr-5">
            <a class="nav-link text-light" href="/get-wishlist"><i class="fa-solid fa-tag"></i> Wish List<span id="wish-count" class="badge rounded-pill bg-success">{{movieWishListcount}}</span></a>
          </li>
          <li class="nav-item active dropdown mr-5">
            <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
              aria-haspopup="true" aria-expanded="false">
              {{#if user}}
                {{user.username}}
                {{else}}
                <i class="fa-solid fa-user"></i>
              {{/if}}
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              {{#if user}}
              <a class="dropdown-item" href="/get-profile">Profile</a>
              <a class="dropdown-item" href="/logout">Logout</a>
              {{else}}
              <a class="dropdown-item" href="/signin">SignIn</a>
              <a class="dropdown-item" href="/login">Login</a>
              {{/if}}
            </div>
          </li>
        </ul>
      </div>
    </nav>
  </header>
  